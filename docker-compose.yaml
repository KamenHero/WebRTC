services:
  backend:
    container_name: backend
    build: ./backend/backend_app
    volumes:
      - ./backend_app:/backend_app
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - '3000:3000'
    volumes:
      - ./frontend:/frontend
